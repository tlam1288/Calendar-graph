const GraphData = require("../graph");

  describe("Graph", () => {

    test("graphData counts how many times an item appears in array", () => {
      expect("7").toMatch(/[0-9]/);
    });

    test("takes days array to create x-axis", () => {
      const days = [];
      expect(days).toStrictEqual([]);
    });

  })